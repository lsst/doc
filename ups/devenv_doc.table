setupRequired(sconsUtils >= 4.7)

# Following is temporarily placed first (vs placing the optionals at end) 
#      since daymops' ups table file does not specify ' >= minimum version'
# Use setupOptional for packages not yet Released but which are privately setup
setupOptional(mops_daymops > 4.1.0.0)

# Following sets up the entire DC3b PT1.1 Release
setupOptional(datarel > 4.1.0.0)
setupOptional(ctrl_orca > 4.1.0.0)
setupOptional(pex_exceptions > 3.2.0)

